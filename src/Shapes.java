
public class Shapes {
	double length;
	double breadth;
	
	//constructor
	Shapes(double x, double y){
		length = x;
		breadth = y;
	}
	
	//calculate area -- nothing is done 
	//because for every shape area is different and so are the number of side etc.
	double area(){
		return 0;
	}
}
